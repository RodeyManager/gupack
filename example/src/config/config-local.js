;(function() {
    var App = {
        constructor: App,
        // 接口host
        ServerHost: '../src/mockData/',
        method: 'POST',
        sendTime: 60,
        debug: true,
        apiSuffix: '.json'
    };

    this['App'] = App;
    return App;

}).call(this);
